* Should Book.name be a string slice
* Should Books in Booklist be an array (because we can instantiate at compile time)?
* Why add Debug trait on Book? For testing? Does this have prod implications?
